"""Configurazione campi traducibili per l'app Core (wagtail-localize).

L'app Core contiene HomePage e MagixSiteSettings.
HomePage non ha campi custom traducibili (solo il title ereditato da Page).
MagixSiteSettings contiene dati non traducibili (telefono, email, API keys).
"""
